# 仿真器 vs 真机：内存差值拆解（2026-07-28）

> **问题**：仿真器与真机的峰值差，到底差在**哪个机制**上？
> **方法**：8 组真机跑每次只改一个因子，故任意两跑之差**隔离出一个物理量**。比较「模型的差」与
> 「真机的差」，就把总残差归因到具体机制，而不是只报一个总比值（总比值会互相抵消而骗人）。
> **数据**：167 / 2026-07-25 / 8 卡 pp4·dp2·ep2 / seq4096 / `peak_alloc_MiB`；真机表由
> `REAL_SHA256=41e279e5…` 指纹保护。复现：`tools/liveness_ab_validate.py` + 本文 §7。

---

## 0. TL;DR —— 差值按**机制**排序

| # | 机制 | 真机 | 模型(bucket) | 误差 | 判定 |
|---|---|---:|---:|---:|---|
| 1 | **重算节省**（fused OFF−ON, s0） | 6238.3 | 21035.1 | **+14796.8** | 模型以为重算省得多得多（**3.4×**） |
| 2 | **unfused fp32 物化**（b−a, s1/s2） | ~29300 | 18865.5 | **−10440** | 聚合欠读 10.4 GiB —— **但见 §5 订正：按层型反号，此聚合值本身是假象** |
| 3 | **每多一层的成本**（a−g，**两跑皆 fused**） | 3885–6252 | 3081–3705 | **−586…−2975** | fused 路径单层成本欠读 **40–50%**（与第 2 项**无关**，见 §5 订正） |
| 4 | **微批 4→8**（e−a, s0） | 1190.2 | 611.5 | −578.7 | 只抓到一半；s1–s3 两侧皆 ≈0（×1 成立） |

**总比值 0.946 是假象**：误差在不同场景**反向**（无重算过读、有重算欠读），互相抵消成一个好看的均值。

---

## 1. 绝对残差（`real − sim`，正 = 模型欠读）

| run | s0 | s1 | s2 | s3 |
|---|---:|---:|---:|---:|
| a fused ON | **+4846.2** | +608.6 | +320.6 | +1266.2 |
| b unfused ON | **+12669.8** | **+11041.4** | **+10764.5** | **+10956.5** |
| c fused **OFF** | **−9950.6** | **−8273.9** | **−4205.0** | +108.5 |
| e fused ON m8 | +5424.9 | +598.4 | +320.7 | +1266.8 |
| f unfused ON m8 | +12594.1 | +11041.9 | +10765.0 | +10957.0 |
| g fused ON **L4** | +1871.2 | −1609.8 | −2227.3 | +680.6 |
| h unfused ON **L4** | −1606.0 | +7239.4 | −2361.4 | +10370.9 |

（`hand_spec` liveness 同表见 §7 脚本输出；其 unfused 残差约为 bucket 的 2/3。）

**读法**：`b` 行几乎恒定欠读 ~11 GiB；`c` 行**过读** 4.2–10.0 GiB。两者方向相反 → 均值掩盖了两个独立的大错。

---

## 2. 核心：四个物理量的误差

### 2.1 重算节省被高估 2.4–3.4×（**最大单项**）

| stage | 真机省 | bucket 省 | hand_spec 省 | err(bucket) |
|---|---:|---:|---:|---:|
| 0 | 6238.3 | 21035.1 | 22771.2 | **+14796.8** |
| 1 | 6377.7 | 15260.2 | 16836.3 | +8882.5 |
| 2 | 3661.4 | 8187.0 | 9507.2 | +4525.6 |
| 3 | 4212.1 | 5369.8 | 5625.8 | +1157.7 |

这一项**由两个独立误差叠加**（见 §1）：无重算端**过读**（−9950.6）+ 有重算端**欠读**（+4846.2）
= 14796.8。所以修任何一端都只能改善一半。

### 2.2 unfused 物化被欠读 ~10.4 GiB

| stage | 真机 | bucket | hand_spec | err(bucket) | err(hand) |
|---|---:|---:|---:|---:|---:|
| 0 | 26034.6 | 18211.0 | 23231.8 | −7823.6 | −2802.8 |
| 1 | 29298.3 | 18865.5 | 23982.3 | **−10432.8** | −5316.0 |
| 2 | 29309.4 | 18865.5 | 23982.3 | −10443.9 | −5327.1 |
| 3 | 24380.1 | 14689.8 | 16346.5 | −9690.3 | −8033.6 |

### 2.3 单层成本欠读 40–50%

`a − g` 隔离出「每 stage 多一层」（L8=2 层/stage vs L4=1 层/stage）：

| stage | 真机 | 模型 | err |
|---|---:|---:|---:|
| 0 | 6056.5 | 3081.5 | −2975.0 |
| 1 | 5773.8 | 3555.4 | −2218.4 |
| 2 | 6252.4 | 3704.5 | −2547.9 |
| 3 | 3885.0 | 3299.4 | −585.6 |

### 2.4 微批深度：×1 成立，但 s0 有真实的深度项

真机 m4→m8 只在 **s0** 移动 1190.2 MiB（s1 −10.2、s2 +0.1、s3 +0.6 ≈ 0）；模型给 611.5。
s0 是 warmup 最深的 stage，**这是唯一残留的深度效应**，模型只抓到约一半。

---

## 3. 残差归因：run b 的欠读是从哪来的

恒等式 `resid(b) = resid(a) + [−err(unfused 机制)]`，实测逐格吻合：

| stage | resid(b) | resid(a) 基线 | 机制引入 | 占比 |
|---|---:|---:|---:|---:|
| 0 | 12669.8 | 4846.2 | 7823.6 | 62% |
| 1 | 11041.4 | 608.6 | **10432.8** | **94%** |
| 2 | 10764.5 | 320.6 | 10443.9 | 97% |
| 3 | 10956.5 | 1266.2 | 9690.3 | 88% |

→ **unfused 的欠读不是基线偏移，是 unfused 机制本身建模错**（中间 stage 高达 94–97%）。

---

## 4. 逐桶归因：残差住在哪个桶里

### 4.1 无重算过读 → 住在 `act_live`

run c stage1：模型 `act_live` **单桶 21987.5 MiB，已超过真机整机峰值 21019.4 MiB**。

按「其余桶正确」反解真机 `act_live`：

| stage | 模型 act_live | 反解真机 act_live | 模型/真机 |
|---|---:|---:|---:|
| 0 | 29812.2 | 19861.6 | **1.50×** |
| 1 | 21987.5 | 13713.7 | **1.60×** |
| 2 | 14658.3 | 10453.4 | **1.40×** |

模型的在途微批深度是 4/3/2（= `pp − stage`，标准 1F1B），单位 = 3664.6 MiB/(层×微批)。
反解真机得 2.71/1.87/1.43 —— **不是整数偏移**，故更像**每层驻留集被高估 ~1.4–1.6×**，
而非「深度算多了一档」。**两者要彻底分辨，需真机逐微批驻留探针**（本轮没有该数据，不硬判）。

### 4.2 unfused 欠读 → **不在** `remat_saves`，在**反向工作集**

run b 各 stage 的 `remat_saves` 已经是 **22441.3 MiB**（模型确实charge了巨量再物化），
但 `bwd_working_set` 只有 **552.1 MiB**。而残差 ~11 GiB。

**量化根因**（源码级，`csa.py:464-533`，seq4096/topk512/d512/b=1）：
`kv_g = cast(kv_gathered, fp32)` 是 `[b,sq,topk,d]` fp32 = **4096 MiB**，且**两个 bmm 都消费它**
→ bprop 物化 `grad_kv_bm`(4096) + `grad_kvo_bm`(4096)，再累加进 `grad_kv_g`(4096)
= **12288 MiB** ≈ 残差量级。

机理：`forward_max_live` **只遍历 `op.inputs`/`op.output`**，而 unfused 那套 fp32 只声明在
`saves` 里 → 对 `fml` 贡献**恰好 0**（实测 r4 层 `fml` 在 fused/unfused **都是 808.1 MiB**，
而 `activation_saves` 是 3703.8 vs 22569.3）。于是唯一的反向工作集项 `fml − bwd_scratch`
**结构上看不见它**。`hand_spec`(liveness·chain2) 因为显式建了内部梯度，残差降到 ~2/3。

---

## 5. 三条量化结论

1. **无重算**：每层驻留激活高估 **~1.4–1.6×**（或深度多算，需探针分辨）。
2. **unfused + 重算**：反向梯度工作集欠读 **~11–12 GiB/层**，等于 gathered-KV fp32 的三份梯度。
3. **单层成本**：欠读 **40–50%**。

> **⚠ 2026-07-28 订正（本文原写"第 3 条与第 2 条同源（unfused 链的梯度侧）"，这是错的）**：
> `a` 与 `g` **两跑都是 fused**（`tools/liveness_ab_validate.py:114,121` 实证 `fused=True`），
> 故 `a − g` 隔离的是 **fused 路径**的单层成本，与 unfused 梯度侧**无关**。它必须单列，
> **不能计入修 (2) 的预期收益**。订正来源：`docs/next_fix_diagnosis_2026-07-28.md`。

> **⚠ 同日第二条订正 —— §2.2「unfused 欠读 ~10.4 GiB」本身也是抵消假象。**
> 在 L4（每 stage 一层）的单层隔离下，误差**按层型反号**：
> `r0_dense` 模型 5243.0 vs 真机 1765.8（**2.97× 过读**）、`r4_moe` 18865.5 vs 27714.7
> （0.68× 欠读）、`r128_moe` 6039.0 vs 5904.9（1.02× 准）。
> 即与 §0 批评的「总比值 0.946 是假象」**同一个病，只是低一层**。
> **后果**：一个"只加字节"的修法（例如让 `fml` 看见 `saves`）会把三个层型**全部推成过读**
> （实测推演 r0→5.94×、r128→2.05×、r4→1.36×）。故 (2) 不可按聚合值直接开修，
> 需先能**逐层型测量**。详见 `docs/next_fix_diagnosis_2026-07-28.md`。

---

## 6. 未解 / 下一步

- **`extracted`（源码抽图）拒绝出数**：级联根 `compressor.py:216`（×4）缺代数恒等式
  `4·(S//4)==S`，导致 unfused 链留在图外（该层 saves **650.5 MiB** vs hand_spec 22045.3 MiB）。
  这是让抽取图能参与本对比的**唯一大石头**。
- **`workspace_bytes`/`bwd_scratch_bytes` 在抽取侧恒为 0**（kernel 内部不在源码里）。这构成
  抽取路径的**结构性天花板**，需要一条**显式来源**的标定透传 —— 是否引入是口径决策，
  引入即意味着把标定值混进「源码真值」。
- **真机逐微批驻留探针**：分辨 §4.1 的「每层过大」vs「深度过深」。
- run d（unfused + 无重算）真机 **OOM**（崩前观测 57712.7–57718.4，**不是**峰值）→ 永久不可评分。

---

## 7. 复现

```bash
cd pynative-cost-evaluator
PYTHONIOENCODING=utf-8 python tools/liveness_ab_validate.py --grad-mode chain2 --deltas
PYTHONIOENCODING=utf-8 python tools/liveness_ab_validate.py --json gate.json
# 本文 §1–§4 的拆解脚本（差分 + 桶截面）：
#   scratchpad/decompose_gap.py（读 gate.json + 直调 Evaluator 取峰值桶截面）
```

真机原始数据：`analysis/realmachine/ab_fusion_2026-07-25/`（两份 base yaml，其余 6 组由
`derive_mf_config` 派生并逐项反查）；原始日志在
`192.168.9.167:/home/suhaibo/workspace/log_ab_fusion_2026-07-25/<run>/worker_<N>.log`。
